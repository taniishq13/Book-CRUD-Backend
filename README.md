# Book CRUD Backend

Simple CRUD app using Node, Express and TypeScript.

## Features
- Create, Read, Update, Delete books
- Pagination
- Sorting

## Run
npm install
npm run dev

## API
POST /books
GET /books
GET /books/:id
PUT /books/:id
DELETE /books/:id
